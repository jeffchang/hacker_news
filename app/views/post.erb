<h3 id="#post-title"><%= @selected_post.title %></h3>
<div id="post-comments" class="container">
  <% @post_comments.each do |comment| %>
    <%= comment.user %>
    <br>
    <%= comment.content %>
  <% end %>
</div>